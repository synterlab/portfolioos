{
  "buildCommand": "pnpm --filter @workspace/api-spec run codegen && pnpm run typecheck:libs && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/portfolio run build && pnpm --filter @workspace/api-server exec node build-vercel.mjs",
  "outputDirectory": "artifacts/portfolio/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
